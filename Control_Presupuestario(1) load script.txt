///$tab Main
SET ThousandSep='.';
SET DecimalSep=',';
SET MoneyThousandSep='.';
SET MoneyDecimalSep=',';
SET MoneyFormat='#.##0,00 €;-#.##0,00 €';
SET TimeFormat='h:mm:ss';
SET DateFormat='DD/MM/YYYY';
SET TimestampFormat='DD/MM/YYYY h:mm:ss[.fff]';
SET FirstWeekDay=0;
SET BrokenWeeks=0;
SET ReferenceDay=4;
SET FirstMonthOfYear=1;
SET CollationLocale='es-ES';
SET CreateSearchIndexOnReload=1;
SET MonthNames='ene.;feb.;mar.;abr.;may.;jun.;jul.;ago.;sep.;oct.;nov.;dic.';
SET LongMonthNames='enero;febrero;marzo;abril;mayo;junio;julio;agosto;septiembre;octubre;noviembre;diciembre';
SET DayNames='lu.;ma.;mi.;ju.;vi.;sá.;do.';
SET LongDayNames='lunes;martes;miércoles;jueves;viernes;sábado;domingo';
SET NumericalAbbreviation='3:k;6:M;9:G;12:T;15:P;18:E;21:Z;24:Y;-3:m;-6:μ;-9:n;-12:p;-15:f;-18:a;-21:z;-24:y';

///$tab Sociedad
Dim_Sociedad:
LOAD
    SociedadID,
    Sociedad
FROM [lib://Datos:DataFiles/Dim_Sociedad.qvd]
(qvd) 

;

///$tab Cuentas Contables
Dim_CuentasContables:
LOAD
    CuentaID,
    NombreDeCuenta,
    CuentaYNombre,
    GrupoDeGasto,
    GrupoDeGastoID
FROM [lib://Datos:DataFiles/Dim_CuentasContables.qvd]
(qvd);



///$tab Proveedores
Dim_proveedores:
LOAD
    MandanteID,
    			ProveedorID,
//     OrganizacionComprasID,
    ProveedorPaisID 		as PaisID,
    			ProveedorNombre,
    ProveedorPoblacion,
    ProveedorCodigoPostal,
//     ProveedorRegion		 as Region,
    ProveedorCalle,
    ProveedorTelefono,
    NumeroIdentificacionFiscal,
    ProveedorNIF,
    ProveedorMoneda,
    CondicionPagoID,
    INCO1,
    FechaCreacion,
    if(Inactivo=0,'Si','No')										as Vigente,
    ProveedorID&'-'&OrganizacionComprasID							as PK_Proveedor,
    ProveedorPaisID&'-'&ProveedorRegion								as PaisRegionID
FROM [lib://Datos:DataFiles/Dim_Proveedores.qvd]
(qvd);


///$tab Maestro Centros Coste
Dim_MaestroCentrosCoste:
LOAD
    CentroCosteID,
    CentroCoste
    
FROM [lib://Datos:DataFiles/Dim_MaestroCentrosCoste.qvd]
(qvd) where IdiomaID = 'S';


///$tab Asientos Contables
// Fact_AsientosContables: 
Hechos:
LOAD
	SociedadID+CuentaID+CentroCosteID+CentroID as PK_Cuenta,
    SociedadID,
    CentroCosteID,
    CentroID,
    CuentaID,
//     CuentaID 				as CuentaIDFiltro,
    'Presupuesto'			as Flag_Presupuesto,
    ImportePresupuesto,
    FechaNum
FROM [lib://Datos:DataFiles/Fact_AsientosContables.qvd]
(qvd);


///$tab Materiales y Artículos Centros Coste
Dim_Materiales:
LOAD
    Referencia,
    Articulo
FROM [lib://Datos:DataFiles/Dim_Materiales.qvd]
(qvd);


Dim_ArticulosCentrosCoste:
LOAD
	//SociedadID+CuentaID+CentroCosteID+CentroID as PK_Cuenta,
    //MandanteID,
    SociedadID as SociedadCuenta,
    CentroID as CentroCuenta,
    CuentaID,
    Referencia as ReferenciaCuenta
    //CentroCosteID
FROM [lib://Datos:DataFiles/Dim_ArticulosCentrosCoste.qvd]
(qvd);

//SociedadID+CuentaID+CentroCosteID+CentroID
///$tab Asientos Contables Centro Coste
// concatenate(Fact_AsientosContables)
concatenate(Hechos)
LOAD
//     CuentaID								as CuentaIDFiltro,
    CuentaID,
    SociedadID,
    CentroID,
    FechaNum,
    CentroCosteID,
    Importe,
    CompraID,
    LineaCompra,
    DenominacionGasto,
    OrdenImputacionID,
    CuentaContrapartidaID,
    CuentaContrapartidaID&'-'&SociedadID						as PK_Proveedor,
    DocumentoContableID,
    [MonedaTransaccionID],
    [MonedaID],
    [ImporteTransaccion],
    [ImporteSociedad],
    'Real'									as Flag_Presupuesto

FROM [lib://Datos:DataFiles/Fact_AsientosContablesCentroCoste.qvd]
(qvd)
;









///$tab Pedidos Compras
// left join(Fact_AsientosContables)
concatenate(Hechos)
Fact_PedidosCompras:
LOAD
    ComprasID   as CompraID,
    LineaCompras  as LineaCompra,
    TipoDocumento,
    FechaCompra,
    UsuarioCreacionPedido,
    FechaCreacionPedido,
    Cantidad,
    Referencia,
    DescripcionTextoBreve,
    OrdenTransporteID,
//     AlmacenID 														as AlmacenDestino,
//     AlmacenEmisor,
    OrganizacionComprasID,
    ComprasID&'-'&LineaCompras										as "PK_Compras",
    ProveedorID&'-'&OrganizacionComprasID							as PK_Proveedor
FROM [lib://Datos:DataFiles/Fact_PedidosComprasControlPresupuestario.qvd]
(qvd);


///$tab Ordenes Transporte
Left Join(Hechos)
Fact_ComprasOT:
LOAD
    //MandanteID,
    EntregaID,
    OrdenTransporteID,
    ClaseDocumTransporte,
    ComprasID    as CompraID,
    LineaCompras as LineaCompra,
    AlmacenID,
    AlmacenEmisorID,
    CentroID,
    SociedadID,
    Referencia
     
FROM [lib://Datos:DataFiles/Fact_ComprasOT.qvd]
(qvd);

 
///$tab Calendario
QuartersMap:  
MAPPING LOAD   
    RowNo() 				as Month,  
    'Q' & Ceil (RowNo()/3) 	as Quarter  
AUTOGENERATE (12);  

Calendario:
load Distinct
	FechaNum											as FechaNum,
    FechaNum											as Fecha,
    Year(FechaNum)										as Año,
    Month(FechaNum)										as Mes,    
	Week(FechaNum) 										as Semana,
	num(Month(FechaNum)) 								as [Nº Mes],  
    Day(FechaNum) 										as Día,  
    Date(MonthStart(FechaNum), 'MMM-YYYY') 				as [Año Mes],
    Num(MonthName(FechaNum)) 							as [NumAñoMes],
    ApplyMap('QuartersMap', Month(FechaNum), Null()) 	as Trimestre, 
    Year(FechaNum) &'/'& Week(FechaNum) 				as [Año Semana],
    Day(FechaNum)&'-'& Month(FechaNum) 					as [Día Mes],
    WeekDay(FechaNum) 									as [Día Semana]  
Resident Hechos;
// Resident Fact_AsientosContables;
///$tab Exit Script
Exit Script;
///$tab Pais
Dim_Pais:
LOAD
    PaisID,
    Pais
FROM [lib://Datos:DataFiles/Dim_Pais.qvd]
(qvd) where IdiomaID = 'S';
 
///$tab Region
Dim_Region:
LOAD
 	PaisID&'-'&RegionID					as PaisRegionID, 
    Region
FROM [lib://Datos:DataFiles/Dim_Region.qvd]
(qvd) where IdiomaID = 'S';
///$tab Transporte
LIB CONNECT TO   'Desarrollo:AzureSQLServer';

[Fact_ComprasOT]:
LOAD //MandanteID, 
	EntregaID, 
	OrdenTransporteID, 
	ClaseDocumTransporte, 
	//ComprasID, 
	//LineaCompras, 
	AlmacenID, 
	AlmacenEmisorID, 
	CentroID 									as CentroIDTransporte
//     , 
// 	SociedadID, 
// 	Referencia
    ;

SELECT MandanteID,
	EntregaID,
	OrdenTransporteID,
	ClaseDocumTransporte,
	ComprasID,
	LineaCompras,
	AlmacenID,
	AlmacenEmisorID,
	CentroID,
	SociedadID,
	Referencia
FROM "BBDD_SQL_Datalake".DATALAKE."Fact_ComprasOT";


///$tab Pedidos Compras
//Esta es una tabla Fact que en esta app opera como Dim

Fact_PedidosCompras:
LOAD
    ComprasID,
	LineaCompras,
	TipoDocumento,
	FechaNum,
	UsuarioCreacionPedido,
	FechaCreacionPedido,
	Cantidad,
    ImporteCompraPedido,
    MonedaImporteCompraPedido,
    ImporteCompraPedidoGrupo,
    MonedaImporteCompraPedidoGrupo,
    ImporteCompraPedidoSociedad,
    MonedaImporteCompraPedidoSociedad,
	Referencia,
	DescripcionTextoBreve,
	OrdenTransporteID,
	AlmacenDestino,
	AlmacenEmisorID	as AlmacenEmisor,
	OrganizacionComprasID,
    ComprasID&'-'&LineaCompras										as "PK_Compras",
    ProveedorID&'-'&OrganizacionComprasID							as PK_Proveedor
    
FROM [lib://Datos:DataFiles/Fact_PedidosCompras.qvd]
(qvd);



///$tab Entregas
//Entregaas se carga para obtener OT


///$tab Maestro Ordenes

///$tab Clientes
/*Clientes:
LOAD
    MandanteID,
    ClienteID,
    OrganizacionVentasID,
    ClienteCanalID,
    ClienteSectorID,
    PaisID,
    ClienteNombre,
    ClientePoblacion,
    ClienteCodigoPostal,
    ClienteRegionID,
    ClienteCalle,
    ClienteTelefono,
    FechaCreacion,
    NumIdentificacionFiscal,
    ClienteNIF,
    ClienteMoneda,
    GrupoCanalID,
    CondicionPagoID,
    INCO1,
    AtencionClienteID,
    AtencionCliente,
    ComercialID,
    Comercial,
    if(Inactivo=0,'Si','No')										as Vigente,
    GrupoID
FROM [lib://Datos:DataFiles/Dim_Clientes.qvd]
(qvd);

*/